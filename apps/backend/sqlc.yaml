version: "2"
sql:
  - engine: sqlite
    schema:
      - internal/platform/database/migrations/0001_initial.sql
    queries:
      - queries
    gen:
      go:
        package: db
        out: internal/store/db
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true

